language: php

php:
  - '5.4'
  - '5.5'
  - '7.0'

install:

before_script:
  - phpize
  - ./configure --with-php-config=php-config
  - make
  - make install
  - make test

script:
  - cd tests && pear run-tests 

after_success:


