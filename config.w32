// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("shadow", "for shadow support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("shadow", "enable shadow support", "no");

if (PHP_SHADOW != "no") {
	EXTENSION("shadow", "shadow.c shadow_cache.c");
}

